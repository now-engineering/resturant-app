export class UserController {
    // async loginUser(loginDto: LoginDto) {
    // 
    //  const data = await axios.post(user ,{headers: ?})
   //  }
}

// OR 
// export const userSlice = createSlice()